Scripts

add_users.sh  -> este script crea los usuarios y contrese;as en los pods, esta dentro de cada pod
add_users2group.sh -> agrega los usuarios al grupo "student" de guacamole
add_userskeycloak.sh -> agrega usuarios a keycloak y los agrega al grupo students de keycloak
create_guacamole_users.sh -> reates users and add them to the group "students" successfully in guacamole, el password no sirve para autenticar 
create_guacamole_users2.sh ->works fine to add users to guacamole, but do not add them to the group "students" hay que usar add_users2group.sh  para complementar
create_users.sh -> crea usuarios en cada pod
